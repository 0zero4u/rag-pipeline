# PDF Parsing
docling>=2.0.0
pdfx>=0.1.0

# RAG Engine
lightrag-hku>=1.0.0

# API Client (OpenAI SDK works with OpenRouter for embeddings, LLM, reranking)
openai>=1.0.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.65.0
